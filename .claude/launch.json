{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "workbench",
            "runtimeExecutable": "php",
            "runtimeArgs": [
                "-r",
                "passthru('php vendor/bin/testbench workbench:build'); passthru('php vendor/bin/testbench serve --port=' . (getenv('PORT') ?: 8000));"
            ],
            "port": 8000,
            "autoPort": true
        }
    ]
}
